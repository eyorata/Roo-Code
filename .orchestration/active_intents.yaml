active_intents:
  - id: "INT-001"
    name: "Hook Middleware Boundary"
    status: "IN_PROGRESS"
    owned_scope:
      - "src/hooks/**"
      - "src/agent/**"
      - "src/orchestration/**"
      - ".orchestration/**"
      - "AGENT.md"
      - "ARCHITECTURE_NOTES.md"
    constraints:
      - "Must enforce intent selection before mutating actions"
      - "Must block writes outside owned_scope"
      - "Must produce content_hash in agent_trace.jsonl"
    acceptance_criteria:
      - "pnpm --filter roo-cline run check-types passes"
      - "write_file events append full trace schema"
  - id: "INT-002"
    name: "Parallel Concurrency Guardrails"
    status: "ACTIVE"
    owned_scope:
      - "src/hooks/**"
      - "src/orchestration/**"
    constraints:
      - "Must reject stale writes when expected hash does not match disk"
      - "Must return recoverable structured errors for blocked operations"
    acceptance_criteria:
      - "Stale file mismatch returns Stale File error"
sessions: {}
